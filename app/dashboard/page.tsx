import { noto } from '@/app/ui/fonts';

export default function Page() {
    return (
        <div className={`${noto.className} text-red-900`} >
            <p> Dashboard Page </p>
        </div>
    )
}